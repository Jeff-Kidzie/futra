---
phase: 01-foundation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ea/FutraEA.mq5
  - ea/include/Common.mqh
  - ea/include/Config.mqh
  - ea/include/Logger.mqh
  - ea/include/KillSwitch.mqh
  - ea/include/OrderManager.mqh
  - ea/include/PositionManager.mqh
  - ea/include/IPCReader.mqh
  - tests/ea/test_helpers.py
  - tests/ea/test_kill_switch.py
  - tests/ea/test_order_execution.py
  - tests/ea/test_position_management.py
  - tests/ea/test_ipc_reader.py
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-08
  - AI-03
  - RISK-01

must_haves:
  truths:
    - "EA compiles and loads in MT5 Strategy Tester without errors"
    - "Kill switch file written to IPC directory halts EA from placing new trades within one tick"
    - "Kill switch with close_positions=true closes all open positions before halting"
    - "Kill switch auto-resets after configured timeout and EA resumes trading"
    - "EA places market buy/sell orders with SL and TP set on every order"
    - "EA reads per-symbol params files (e.g. EURUSD_params.json) and uses AI parameters when available"
    - "EA falls back to hardcoded safe defaults from Config.mqh when params file is missing or stale"
    - "Every trade result (success or error) is logged to a trade log file with retcode, comment, and timestamp"
    - "All tests pass without a live MT5 connection (IPC integration tests with mock files)"
  artifacts:
    - path: "ea/FutraEA.mq5"
      provides: "Main EA entry point — wires all modules, runs OnTick loop"
      min_lines: 80
    - path: "ea/include/Common.mqh"
      provides: "Shared enums (OrderType, KillSwitchState, TradeDirection), structs (TradeResult, PositionInfo), and IPC path constants"
      min_lines: 60
    - path: "ea/include/Config.mqh"
      provides: "Hardcoded safe default parameters (SL%, TP%, maxPositionSize, killSwitchTimeout), symbol lists, polling intervals"
      contains: "input double"
    - path: "ea/include/Logger.mqh"
      provides: "Trade logging to file — writes TradeResult structs as JSON lines to ipc/trade_log.jsonl"
      exports: ["LogTrade", "LogError", "LogInfo"]
    - path: "ea/include/KillSwitch.mqh"
      provides: "Kill switch file polling, close_positions flag handling, auto-reset timeout"
      exports: ["CheckKillSwitch", "IsKillSwitchActive"]
    - path: "ea/include/OrderManager.mqh"
      provides: "Market order execution with SL/TP, filling type auto-detection"
      exports: ["OpenBuyOrder", "OpenSellOrder"]
    - path: "ea/include/PositionManager.mqh"
      provides: "Position close (by ticket), close all positions, modify SL/TP"
      exports: ["ClosePosition", "CloseAllPositions", "ModifySLTP"]
    - path: "ea/include/IPCReader.mqh"
      provides: "Read per-symbol JSON params files with staleness check (timestamp comparison)"
      exports: ["ReadSymbolParams", "IsParamsStale"]
    - path: "tests/ea/test_helpers.py"
      provides: "IPC test utilities — write params/killswitch files, read EA log output, cleanup IPC dir"
      min_lines: 50
    - path: "tests/ea/test_kill_switch.py"
      provides: "Kill switch integration tests"
      min_lines: 50
    - path: "tests/ea/test_order_execution.py"
      provides: "Market order execution integration tests"
      min_lines: 40
    - path: "tests/ea/test_position_management.py"
      provides: "Position close/modify integration tests"
      min_lines: 40
    - path: "tests/ea/test_ipc_reader.py"
      provides: "IPC params reading and staleness check tests"
      min_lines: 40
  key_links:
    - from: "ea/FutraEA.mq5 OnTick()"
      to: "KillSwitch.mqh::CheckKillSwitch()"
      via: "function call every tick"
      pattern: "CheckKillSwitch"
    - from: "ea/FutraEA.mq5 OnTick()"
      to: "IPCReader.mqh::ReadSymbolParams()"
      via: "per-symbol params read with staleness check"
      pattern: "ReadSymbolParams"
    - from: "ea/FutraEA.mq5 OnTick()"
      to: "OrderManager.mqh::OpenBuyOrder/OpenSellOrder"
      via: "trading signal path when kill switch inactive and params available"
      pattern: "OpenBuyOrder|OpenSellOrder"
    - from: "OrderManager.mqh"
      to: "Logger.mqh::LogTrade()"
      via: "log after every OrderSend result"
      pattern: "LogTrade"
    - from: "Config.mqh safe defaults"
      to: "OrderManager.mqh SL/TP calculation"
      via: "fallback when IPCReader returns stale/empty params"
      pattern: "SafeDefaultSL|SafeDefaultTP"
---

<objective>
Build the MQL5 Expert Advisor core: kill switch with file-based polling, market order execution with SL/TP on every order, position management (close/modify), per-symbol IPC params reading with staleness check, trade logging, and hardcoded safe defaults that keep the EA running when AI parameters are unavailable.

Purpose: The EA is the execution authority — it must trade safely with no AI dependency, halt instantly on kill switch activation, and log every action for audit. This plan creates the EA skeleton and all core modules.

Output: Compilable MQL5 EA with 6 include modules, 4 Python test files for IPC integration testing, and a working kill switch that can be tested without MT5 running.
</objective>

<execution_context>
@C:/Users/jeffr/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/jeffr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-safety/01-CONTEXT.md

<interfaces>
<!-- IPC Contract — defined here, consumed by Plan 01-02 -->
<!-- All MQL5 includes use these paths and formats -->

IPC Directory: `{terminal_data_path}/MQL5/Files/Futra/`
Kill switch file: `Futra/kill_switch.json`
Per-symbol params: `Futra/{SYMBOL}_params.json`
Trade log: `Futra/trade_log.jsonl`
EA state output: `Futra/ea_state.json`

Kill switch format:
```json
{
  "active": true,
  "close_positions": true,
  "reason": "manual_emergency",
  "timestamp": "2026-05-24T12:00:00Z"
}
```

Per-symbol params format:
```json
{
  "symbol": "EURUSD",
  "timestamp": "2026-05-24T12:00:00Z",
  "sl_percent": 0.02,
  "tp_percent": 0.04,
  "max_position_size": 0.1,
  "regime": "trending",
  "confidence": 0.85
}
```

Trade log line format (JSONL):
```json
{"ticket":12345,"symbol":"EURUSD","type":"buy","volume":0.1,"price":1.0850,"sl":1.0830,"tp":1.0890,"retcode":10009,"comment":"","timestamp":"2026-05-24T12:00:01Z"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding, IPC contract, Config/Common modules, and test infrastructure</name>
  <files>
    ea/FutraEA.mq5
    ea/include/Common.mqh
    ea/include/Config.mqh
    tests/ea/test_helpers.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-safety/01-CONTEXT.md (all D-XX decisions)
    - .planning/REQUIREMENTS.md (DATA-04, DATA-05, DATA-08, AI-03, RISK-01 requirements)
  </read_first>
  <behavior>
    - Test 1: test_helpers.py can create IPC directory, write a kill_switch.json file, read ea_state.json, and clean up IPC directory
    - Test 2: Config.mqh exports input double SafeDefaultSLPercent = 2.0, SafeDefaultTPPercent = 4.0, MaxPositionSize = 0.1, KillSwitchTimeoutMinutes = 30, and string IpcDirectory = "Futra/"
    - Test 3: Common.mqh defines enum ENUM_KILL_SWITCH_STATE (INACTIVE, ACTIVE_NO_CLOSE, ACTIVE_CLOSE_ALL), struct TradeResult (ticket, symbol, type, volume, price, sl, tp, retcode, comment, timestamp), struct PositionInfo (ticket, symbol, type, volume, openPrice, sl, tp, profit), string IpcPath constant
    - Test 4: FutraEA.mq5 compiles (just skeleton with #include of all module headers, OnInit/OnDeinit/OnTick stubs, no logic yet)
  </behavior>
  <action>
    Create the EA project structure and foundation modules:

    **1. `ea/include/Common.mqh`** — Shared types and constants:
    - `#property strict` at top
    - Enum `ENUM_KILL_SWITCH_STATE` with values: `KS_INACTIVE`, `KS_ACTIVE_NO_CLOSE`, `KS_ACTIVE_CLOSE_ALL`
    - Enum `ENUM_TRADE_DIRECTION` with values: `TRADE_BUY`, `TRADE_SELL`
    - Struct `TradeResult` with fields: `ulong ticket`, `string symbol`, `string type`, `double volume`, `double price`, `double sl`, `double tp`, `int retcode`, `string comment`, `datetime timestamp`
    - Struct `PositionInfo` with fields: `ulong ticket`, `string symbol`, `int type`, `double volume`, `double openPrice`, `double sl`, `double tp`, `double profit`
    - Constant string `IPC_DIRECTORY = "Futra/"` (relative to MQL5/Files/)
    - Constant string `KILL_SWITCH_FILE = "Futra/kill_switch.json"`
    - Constant string `TRADE_LOG_FILE = "Futra/trade_log.jsonl"`
    - Constant string `EA_STATE_FILE = "Futra/ea_state.json"`

    **2. `ea/include/Config.mqh`** — Hardcoded safe defaults (per AI-03):
    - `#property strict` and `#include "Common.mqh"`
    - `input double InpSafeDefaultSLPercent = 2.0;` — when AI params unavailable, set SL at 2% of entry price
    - `input double InpSafeDefaultTPPercent = 4.0;` — when AI params unavailable, set TP at 4% of entry price
    - `input double InpMaxPositionSize = 0.1;` — max lot size per trade
    - `input int InpKillSwitchTimeoutMinutes = 30;` — auto-reset timeout per D-04
    - `input int InpParamsStalenessSeconds = 60;` — stale threshold for params files per D-06
    - `input string InpSymbols = "EURUSD,GBPUSD,USDJPY";` — comma-separated trading symbols
    - `input int InpMagicNumber = 20260501;` — unique EA identifier for order filtering

    **3. `ea/FutraEA.mq5`** — Main EA skeleton:
    - `#property copyright`, `#property link`, `#property version "1.0"`
    - `#include "include/Common.mqh"`
    - `#include "include/Config.mqh"`
    - `#include "include/Logger.mqh"`
    - `#include "include/KillSwitch.mqh"`
    - `#include "include/OrderManager.mqh"`
    - `#include "include/PositionManager.mqh"`
    - `#include "include/IPCReader.mqh"`
    - `int OnInit()` — return `INIT_SUCCEEDED` (stub, wiring comes in Task 3)
    - `void OnDeinit(const int reason)` — stub
    - `void OnTick()` — stub (wiring comes in Task 3)

    **4. `tests/ea/test_helpers.py`** — Python test utilities:
    - Function `create_ipc_dir(base_path: Path) -> Path` — creates `{base_path}/Futra/` directory
    - Function `cleanup_ipc_dir(base_path: Path)` — removes `{base_path}/Futra/` directory
    - Function `write_kill_switch(base_path: Path, active: bool, close_positions: bool, reason: str = "test") -> Path` — writes kill_switch.json to IPC dir, returns file path. JSON format: `{"active": bool, "close_positions": bool, "reason": str, "timestamp": ISO8601_str}`
    - Function `write_symbol_params(base_path: Path, symbol: str, sl_percent: float, tp_percent: float, max_position_size: float, regime: str = "trending", confidence: float = 0.85) -> Path` — writes `{SYMBOL}_params.json`. JSON format per IPC contract
    - Function `read_trade_log(base_path: Path) -> list[dict]` — reads trade_log.jsonl, returns list of parsed JSON objects (one per line)
    - Function `read_ea_state(base_path: Path) -> dict | None` — reads ea_state.json if exists
    - `pytest.fixture` `temp_ipc_dir(tmp_path)` — creates temp IPC base dir, yields Path, cleanup_ipc_dir in teardown
  </action>
  <verify>
    <automated>python -c "from tests.ea.test_helpers import create_ipc_dir, cleanup_ipc_dir, write_kill_switch, write_symbol_params, read_trade_log; print('Imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `ea/include/Common.mqh` exists with ENUM_KILL_SWITCH_STATE, TradeResult struct, PositionInfo struct, IPC path constants — verify: `grep "ENUM_KILL_SWITCH_STATE\|struct TradeResult\|IPC_DIRECTORY" ea/include/Common.mqh`
    - [ ] `ea/include/Config.mqh` exists with all input double/int/string parameters listed above — verify: `grep "InpSafeDefaultSLPercent\|InpKillSwitchTimeoutMinutes\|InpMagicNumber" ea/include/Config.mqh`
    - [ ] `ea/FutraEA.mq5` exists with all 6 includes, OnInit/OnDeinit/OnTick stubs — verify: `grep "#include" ea/FutraEA.mq5 | wc -l` returns 6
    - [ ] `tests/ea/test_helpers.py` exists with all 6 functions and temp_ipc_dir fixture — verify: `grep "def create_ipc_dir\|def write_kill_switch\|def write_symbol_params\|def read_trade_log\|def read_ea_state\|def cleanup_ipc_dir" tests/ea/test_helpers.py`
  </acceptance_criteria>
  <done>All files committed, EA skeleton compiles in MT5 (or at minimum has correct syntax), test helpers import successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kill switch module and Logger module with integration tests</name>
  <files>
    ea/include/Logger.mqh
    ea/include/KillSwitch.mqh
    tests/ea/test_kill_switch.py
  </files>
  <read_first>
    - ea/include/Common.mqh (IPC path constants, TradeResult struct)
    - ea/include/Config.mqh (InpKillSwitchTimeoutMinutes)
    - tests/ea/test_helpers.py (write_kill_switch, read_trade_log, temp_ipc_dir)
  </read_first>
  <behavior>
    - Test 1: write kill_switch.json with active=true, close_positions=true → EA detects kill switch active, state is KS_ACTIVE_CLOSE_ALL
    - Test 2: write kill_switch.json with active=true, close_positions=false → EA detects kill switch active, state is KS_ACTIVE_NO_CLOSE
    - Test 3: no kill_switch.json file exists → EA reports KS_INACTIVE
    - Test 4: kill_switch.json is malformed (invalid JSON) → EA logs error, treats as KS_INACTIVE (safe default: never halt on parse error)
    - Test 5: kill_switch.json with auto-reset — write file, note timestamp, wait for period less than timeout → still active; verify after timeout → EA treats as INACTIVE (timeout exceeded)
    - Test 6: Logger writes a TradeResult to trade_log.jsonl → file contains valid JSON line matching the struct fields
    - Test 7: Logger handles file write error gracefully (no crash, returns error status)
  </behavior>
  <action>
    Implement kill switch and logging per D-01, D-02, D-04, and DATA-08:

    **1. `ea/include/Logger.mqh`** — Trade and error logging:
    - `#property strict` and `#include "Common.mqh"`
    - `void LogTrade(TradeResult &result)` — appends TradeResult as JSON line to `IPC_DIRECTORY + TRADE_LOG_FILE`. Uses `FileOpen` with `FILE_TXT|FILE_READ|FILE_WRITE|FILE_SHARE_READ`, seeks to end with `FileSeek(handle, 0, SEEK_END)`. Builds JSON string manually (MQL5 has no JSON library): format as `{"ticket":{ticket},"symbol":"{symbol}","type":"{type}","volume":{volume},"price":{price},"sl":{sl},"tp":{tp},"retcode":{retcode},"comment":"{comment}","timestamp":"{timestamp}"}`. On `FileOpen` failure, calls `Print()` with error details (no crash).
    - `void LogError(string context, int errorCode, string details)` — appends error JSON line: `{"level":"error","context":"{context}","errorCode":{errorCode},"details":"{details}","timestamp":"{timestamp}"}`. Uses same file append pattern as LogTrade.
    - `void LogInfo(string message)` — appends info JSON line: `{"level":"info","message":"{message}","timestamp":"{timestamp}"}`.
    - Helper: `string GetCurrentTimestamp()` — returns ISO8601-style string from `TimeCurrent()` using `TimeToString(TimeCurrent(), TIME_DATE|TIME_SECONDS)`. Format: "2026-05-24 12:00:00" (MT5 doesn't do ISO8601 natively, use `StringFormat("%sT%sZ", TimeToString(t, TIME_DATE), TimeToString(t, TIME_SECONDS))` pattern).

    **2. `ea/include/KillSwitch.mqh`** — Kill switch logic per D-01, D-02, D-04:
    - `#property strict` and `#include "Common.mqh"` and `#include "Config.mqh"` and `#include "Logger.mqh"`
    - Module-level static variables: `static bool s_killSwitchActive = false;`, `static bool s_closePositions = false;`, `static datetime s_killSwitchActivatedAt = 0;`, `static datetime s_lastKillSwitchCheck = 0;`
    - `ENUM_KILL_SWITCH_STATE CheckKillSwitch()` — called every tick from EA OnTick:
      - If less than 500ms since last check, return cached state (rate-limit file I/O)
      - Update `s_lastKillSwitchCheck = TimeCurrent()`
      - Try `FileOpen(IPC_DIRECTORY + KILL_SWITCH_FILE, FILE_TXT|FILE_READ|FILE_SHARE_READ)`
      - If `FileOpen` returns `INVALID_HANDLE`: file doesn't exist → if kill switch was previously active, check timeout. If timeout exceeded (per D-04, use `InpKillSwitchTimeoutMinutes`), reset to INACTIVE, log "Kill switch auto-reset after timeout". Return `KS_INACTIVE`.
      - If file opens: read full content with `FileReadString(handle)` to get JSON string
      - Parse JSON manually: search for `"active"` key → extract `true`/`false` after colon. Search for `"close_positions"` key → extract `true`/`false` after colon.
      - If `active: true`: set `s_killSwitchActive = true`, store `s_killSwitchActivatedAt = TimeCurrent()`, set `s_closePositions = close_positions` value. Log "Kill switch activated: close_positions={val}". Return `KS_ACTIVE_CLOSE_ALL` or `KS_ACTIVE_NO_CLOSE`.
      - If `active: false`: treat as inactive, reset state, return `KS_INACTIVE`.
      - If JSON parsing fails (can't find "active" key): LogError "Kill switch file parse error", treat as `KS_INACTIVE` (safe default — never halt on bad data per AI-03).
    - `bool IsKillSwitchActive()` — returns `s_killSwitchActive` (for other modules to check quickly)
    - `bool ShouldClosePositions()` — returns `s_closePositions` (for EA to know whether to close positions on kill switch)
    - `void ResetKillSwitch()` — resets all static state to inactive, logs "Kill switch manually reset"

    **3. `tests/ea/test_kill_switch.py`** — IPC integration tests for kill switch:
    - Test `test_kill_switch_active_with_close`: write kill_switch.json with active=true, close_positions=true → verify file content matches expected JSON structure. This tests the contract (EA reads same format). Document: "EA verifies this format by reading the file — test confirms correct file content."
    - Test `test_kill_switch_active_no_close`: write with active=true, close_positions=false → verify JSON has correct close_positions value.
    - Test `test_kill_switch_inactive`: write with active=false → verify JSON has `"active": false`.
    - Test `test_kill_switch_missing_file`: delete kill_switch.json → verify file doesn't exist (EA should treat as inactive).
    - Test `test_kill_switch_malformed`: write invalid JSON (missing closing brace) → verify file exists with bad content (EA should handle gracefully).
    - Test `test_kill_switch_timeout`: write kill_switch.json with a past timestamp → verify timestamp parseable. Document: "EA auto-resets after InpKillSwitchTimeoutMinutes — test verifies file timestamp is writable in expected format."
    - Test `test_trade_log_written`: write a trade entry JSON line to trade_log.jsonl → read back with test_helpers.read_trade_log() → verify fields match.
    Each test uses `temp_ipc_dir` fixture and test_helpers functions. Tests verify FILE CONTENT, not EA behavior (EA testing requires MT5 runtime which Python can't do — this is the tested-at-contract-boundary approach per D-10).
  </action>
  <verify>
    <automated>python -m pytest tests/ea/test_kill_switch.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `ea/include/Logger.mqh` exists with LogTrade, LogError, LogInfo, GetCurrentTimestamp functions — verify: `grep "void LogTrade\|void LogError\|void LogInfo\|string GetCurrentTimestamp" ea/include/Logger.mqh`
    - [ ] `ea/include/KillSwitch.mqh` exists with CheckKillSwitch returning ENUM_KILL_SWITCH_STATE — verify: `grep "ENUM_KILL_SWITCH_STATE CheckKillSwitch\|bool IsKillSwitchActive\|bool ShouldClosePositions" ea/include/KillSwitch.mqh`
    - [ ] Kill switch reads from `IPC_DIRECTORY + KILL_SWITCH_FILE` path — verify: `grep "KILL_SWITCH_FILE" ea/include/KillSwitch.mqh`
    - [ ] Auto-reset timeout uses `InpKillSwitchTimeoutMinutes` from Config — verify: `grep "InpKillSwitchTimeoutMinutes" ea/include/KillSwitch.mqh`
    - [ ] All 7 kill switch tests + 1 log test pass — verify: `python -m pytest tests/ea/test_kill_switch.py -v` exits 0
  </acceptance_criteria>
  <done>Kill switch module implemented with file polling, close_positions flag, auto-reset timeout, and JSON parse error handling. Logger writes structured JSONL. All Python contract tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OrderManager, PositionManager, IPCReader, and main EA wiring</name>
  <files>
    ea/FutraEA.mq5
    ea/include/OrderManager.mqh
    ea/include/PositionManager.mqh
    ea/include/IPCReader.mqh
    tests/ea/test_order_execution.py
    tests/ea/test_position_management.py
    tests/ea/test_ipc_reader.py
  </files>
  <read_first>
    - ea/include/Common.mqh (TradeResult, PositionInfo structs, IPC path constants)
    - ea/include/Config.mqh (InpSafeDefaultSLPercent, InpSafeDefaultTPPercent, InpMaxPositionSize, InpSymbols, InpMagicNumber, InpParamsStalenessSeconds)
    - ea/include/Logger.mqh (LogTrade, LogError, LogInfo signatures)
    - ea/include/KillSwitch.mqh (CheckKillSwitch, IsKillSwitchActive, ShouldClosePositions signatures)
    - tests/ea/test_helpers.py (write_symbol_params, read_trade_log, read_ea_state, temp_ipc_dir)
  </read_first>
  <behavior>
    - Test 1: write EURUSD_params.json with sl_percent=1.5, tp_percent=3.0 → verify JSON file content matches contract
    - Test 2: write params file, then immediately modify it with new timestamp → verify file mtime differs (EA staleness check should detect)
    - Test 3: no params file exists → verify missing (EA should use safe defaults)
    - Test 4: params file with stale timestamp (> InpParamsStalenessSeconds old) → verify timestamp comparison would flag as stale
    - Test 5: write a complete trade log entry after simulated order → verify JSONL format matches TradeResult struct fields
    - Test 6: position modification — verify the SL/TP fields in a params json can be set and read back correctly
  </behavior>
  <action>
    Implement order execution, position management, IPC params reading, and wire everything in the main EA per D-05 (Python has own MT5 connection — EA only reads, doesn't push data), D-06 (staleness check), D-08 (per-symbol params), and requirements DATA-04, DATA-05, DATA-06, AI-03:

    **1. `ea/include/IPCReader.mqh`** — Per-symbol params file reading with staleness check per D-06, D-08:
    - `#property strict` and `#include "Common.mqh"` and `#include "Config.mqh"`
    - Struct `SymbolParams` with fields: `string symbol`, `datetime timestamp`, `double slPercent`, `double tpPercent`, `double maxPositionSize`, `string regime`, `double confidence`, `bool isFresh` (true if timestamp within staleness window)
    - `SymbolParams ReadSymbolParams(string symbol)` — reads `IPC_DIRECTORY + symbol + "_params.json"`:
      - Build path: `StringFormat("%s%s_params.json", IPC_DIRECTORY, symbol)`
      - Try `FileOpen(path, FILE_TXT|FILE_READ|FILE_SHARE_READ)`
      - If `INVALID_HANDLE`: return SymbolParams with `isFresh = false`, all values zeroed (caller falls back to Config defaults per AI-03)
      - Read full file content with `FileReadString(handle)`, close handle
      - Parse JSON manually: extract `"timestamp"`, `"sl_percent"`, `"tp_percent"`, `"max_position_size"`, `"regime"`, `"confidence"` values using string search
      - Parse timestamp string to `datetime` using `StringToTime()`
      - Compare against `TimeCurrent()`: if `TimeCurrent() - fileTimestamp > InpParamsStalenessSeconds`, set `isFresh = false` (stale per D-06)
      - Otherwise set `isFresh = true` and populate all fields
      - If JSON parse fails at any point: set `isFresh = false`, log warning, return zeroed struct
      - `FileClose(handle)` before returning
    - `bool IsParamsFresh(SymbolParams &params)` — convenience: returns `params.isFresh`
    - Module-level cache: `static SymbolParams s_cachedParams[]` and `static datetime s_lastReadTime[]` per symbol to avoid re-reading on every tick. Cache check: if `TimeCurrent() - s_lastReadTime[symbolIndex] < 1` (1 second throttle), return cached.

    **2. `ea/include/OrderManager.mqh`** — Market order execution with SL/TP per DATA-04, DATA-05:
    - `#property strict` and `#include "Common.mqh"` and `#include "Config.mqh"` and `#include "Logger.mqh"`
    - `TradeResult OpenBuyOrder(string symbol, double volume, double slPercent = 0, double tpPercent = 0)` — opens a market buy order:
      - If `slPercent == 0` use `InpSafeDefaultSLPercent`; if `tpPercent == 0` use `InpSafeDefaultTPPercent` (safe defaults per AI-03)
      - Get current price: `double ask = SymbolInfoDouble(symbol, SYMBOL_ASK)`; if ask == 0, return TradeResult with retcode = -1, comment = "SymbolInfoDouble failed"
      - Calculate SL/TP: `sl = ask * (1.0 - slPercent / 100.0)`, `tp = ask * (1.0 + tpPercent / 100.0)`
      - Normalize price and SL/TP to symbol digits: `int digits = (int)SymbolInfoInteger(symbol, SYMBOL_DIGITS)`, `sl = NormalizeDouble(sl, digits)`, `tp = NormalizeDouble(tp, digits)`
      - Get filling type: `long fillMode = SymbolInfoInteger(symbol, SYMBOL_FILLING_MODE)`. Try `ORDER_FILLING_FOK` first, then `ORDER_FILLING_IOC`, then `ORDER_FILLING_RETURN`. Per DATA-04 — auto-detect supported filling per symbol.
      - Build `MqlTradeRequest`:
        - `action = TRADE_ACTION_DEAL`, `symbol = symbol`, `volume = volume`, `type = ORDER_TYPE_BUY`, `price = ask`, `sl = sl`, `tp = tp`, `deviation = 20`, `magic = InpMagicNumber`, `comment = "Futra"`
      - `MqlTradeResult result;` `OrderSend(request, result)`
      - Build TradeResult from MqlTradeResult fields: `ticket = result.order`, `symbol = symbol`, `type = "buy"`, `volume = volume`, `price = result.price`, `sl = sl`, `tp = tp`, `retcode = result.retcode`, `comment = result.comment`, `timestamp = TimeCurrent()`
      - Call `LogTrade(tradeResult)` (per DATA-08 — log every trade result)
      - If retcode != `TRADE_RETCODE_DONE` (10009): also call `LogError("OpenBuyOrder", result.retcode, "OrderSend failed")`
      - Return tradeResult
    - `TradeResult OpenSellOrder(string symbol, double volume, double slPercent = 0, double tpPercent = 0)` — symmetric to OpenBuyOrder:
      - Use `SymbolInfoDouble(symbol, SYMBOL_BID)` for current price
      - `sl = bid * (1.0 + slPercent / 100.0)`, `tp = bid * (1.0 - tpPercent / 100.0)` (inverted for sell)
      - `type = ORDER_TYPE_SELL`
      - All other logic identical to OpenBuyOrder
    - `double GetDefaultVolume(string symbol)` — returns `InpMaxPositionSize` clamped to `SymbolInfoDouble(symbol, SYMBOL_VOLUME_MIN)` and `SYMBOL_VOLUME_MAX`, normalized to `SymbolInfoInteger(symbol, SYMBOL_VOLUME_STEP)`

    **3. `ea/include/PositionManager.mqh`** — Position management per DATA-06:
    - `#property strict` and `#include "Common.mqh"` and `#include "Logger.mqh"`
    - `bool ClosePosition(ulong ticket)` — closes a position by ticket:
      - `PositionSelectByTicket(ticket)` — if false, return false (position not found)
      - Get position details: `symbol`, `volume`, `type` (POSITION_TYPE_BUY or POSITION_TYPE_SELL)
      - Determine close order type: if position is buy → `ORDER_TYPE_SELL`; if sell → `ORDER_TYPE_BUY`
      - Get close price: if buy position → `SymbolInfoDouble(symbol, SYMBOL_BID)`; if sell → `SymbolInfoDouble(symbol, SYMBOL_ASK)`
      - Build MqlTradeRequest with `action = TRADE_ACTION_DEAL`, `position = ticket`, appropriate type/price/volume
      - `OrderSend(request, result)`
      - Log result
      - Return true if retcode == `TRADE_RETCODE_DONE`
    - `int CloseAllPositions()` — iterates all positions with `InpMagicNumber`:
      - Loop `for(int i = PositionsTotal() - 1; i >= 0; i--)` (reverse to handle closing)
      - `PositionSelectByTicket(PositionGetTicket(i))`
      - Check `PositionGetInteger(POSITION_MAGIC) == InpMagicNumber`
      - If match: `ClosePosition(PositionGetTicket(i))`
      - Return count of positions attempted to close
    - `bool ModifySLTP(ulong ticket, double newSL, double newTP)` — modifies SL/TP on existing position:
      - `PositionSelectByTicket(ticket)`
      - Get `symbol`, normalize prices
      - Build MqlTradeRequest with `action = TRADE_ACTION_SLTP`, `position = ticket`, `sl = newSL`, `tp = newTP`
      - `OrderSend`, log, return true on success
    - `void GetPositions(PositionInfo &positions[], int &count)` — fills array with all positions matching magic number:
      - Count positions first pass, resize array, fill second pass

    **4. `ea/FutraEA.mq5`** — Wire everything in OnTick per the EA's execution loop:
    - `void OnTick()`:
      1. `ENUM_KILL_SWITCH_STATE ks = CheckKillSwitch()` — check kill switch first every tick per D-01
      2. If `ShouldClosePositions()`: call `CloseAllPositions()`, log "Kill switch: positions closed", `return` (halt trading per D-02)
      3. If `IsKillSwitchActive()`: `return` (halt new trades but don't close positions if close_positions was false)
      4. Parse `InpSymbols` comma-separated list: `StringSplit(InpSymbols, ',', symbols)`
      5. For each symbol: `SymbolParams params = ReadSymbolParams(symbols[i])`
      6. If `IsParamsFresh(params)`: use `params.slPercent` and `params.tpPercent`; else use `InpSafeDefaultSLPercent` and `InpSafeDefaultTPPercent` (AI-03 safe defaults)
      7. Trading signal determination: placeholder for now — the EA frame is ready for signals to be connected. Document with comment: "// Trading signal logic will be implemented in a future phase. EA core is ready to execute when signals arrive."
      8. On deinit: `void OnDeinit(const int reason)` — log "EA shutting down, reason: {reason}", close any open file handles

    **5. Tests** — `tests/ea/test_order_execution.py`, `tests/ea/test_position_management.py`, `tests/ea/test_ipc_reader.py`:
    - `test_ipc_reader.py`: Test params file format — write `{symbol}_params.json` with all fields → verify JSON content. Test stale detection — write file with old timestamp → verify mtime. Test missing file → verify no file. Test malformed JSON → verify bad content.
    - `test_order_execution.py`: Test the contract that EA uses — verify trade log entry format matches TradeResult. Write a mock trade log line → read back → verify all fields present (ticket, symbol, type, volume, price, sl, tp, retcode, comment, timestamp).
    - `test_position_management.py`: Test the contract for position close — verify TradeResult struct serialization. Test CloseAllPositions log format.
    All tests use `temp_ipc_dir` fixture. Tests verify FILE CONTENT correctness (the contract EA implements), not runtime EA behavior per D-10 and D-11.
  </action>
  <verify>
    <automated>python -m pytest tests/ea/test_order_execution.py tests/ea/test_position_management.py tests/ea/test_ipc_reader.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `ea/include/IPCReader.mqh` exists with ReadSymbolParams returning SymbolParams struct with isFresh flag — verify: `grep "SymbolParams ReadSymbolParams\|struct SymbolParams\|IsParamsFresh\|InpParamsStalenessSeconds" ea/include/IPCReader.mqh`
    - [ ] `ea/include/OrderManager.mqh` exists with OpenBuyOrder and OpenSellOrder returning TradeResult — verify: `grep "TradeResult OpenBuyOrder\|TradeResult OpenSellOrder" ea/include/OrderManager.mqh`
    - [ ] OpenBuyOrder uses InpSafeDefaultSLPercent when slPercent=0 — verify: `grep "InpSafeDefaultSLPercent" ea/include/OrderManager.mqh`
    - [ ] OpenBuyOrder detects filling type from SymbolInfoInteger(SYMBOL_FILLING_MODE) — verify: `grep "SYMBOL_FILLING_MODE" ea/include/OrderManager.mqh`
    - [ ] OpenBuyOrder calls LogTrade after OrderSend — verify: `grep "LogTrade" ea/include/OrderManager.mqh`
    - [ ] `ea/include/PositionManager.mqh` exists with ClosePosition, CloseAllPositions, ModifySLTP — verify: `grep "bool ClosePosition\|int CloseAllPositions\|bool ModifySLTP" ea/include/PositionManager.mqh`
    - [ ] `ea/FutraEA.mq5` OnTick calls CheckKillSwitch first, then reads params per symbol — verify: `grep "CheckKillSwitch" ea/FutraEA.mq5` and `grep "ReadSymbolParams" ea/FutraEA.mq5`
    - [ ] OnTick respects kill switch: returns early when active — verify: `grep "IsKillSwitchActive\|ShouldClosePositions" ea/FutraEA.mq5`
    - [ ] All Python integration contract tests pass — verify: `python -m pytest tests/ea/ -v` exits 0
  </acceptance_criteria>
  <done>EA core is fully wired: kill switch checked every tick, params read per symbol with staleness detection, order execution with auto-detected filling types, SL/TP on every order, trade logging, safe defaults fallback. All Python contract tests pass. EA compiles in MT5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC filesystem → EA | EA reads untrusted JSON files from IPC directory. Malformed or malicious content must not crash the EA. |
| EA → MT5 Terminal | EA runs inside MT5 process. OrderSend operations are the execution authority — no bypass of MT5's own safeguards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | KillSwitch.mqh | mitigate | Malformed kill_switch.json: if JSON parsing fails (can't find "active" key), treat as KS_INACTIVE — never halt trading on bad data. Log error via Logger. |
| T-01-02 | Tampering | IPCReader.mqh | mitigate | Malformed per-symbol params JSON: if reading or parsing fails, set isFresh=false and return zeroed struct. EA falls back to hardcoded safe defaults (AI-03) — never use corrupted parameters. |
| T-01-03 | Denial of Service | KillSwitch.mqh | mitigate | Missing kill switch file: treat as inactive. File I/O rate-limited to 500ms between checks to prevent tick-flood file operations. |
| T-01-04 | Information Disclosure | Logger.mqh | accept | Trade log written to local filesystem within MT5 data directory. No PII or credentials logged. Local-only access at this phase (no web dashboard yet). |
| T-01-05 | Elevation of Privilege | KillSwitch.mqh | accept | Kill switch file is in MT5/Files/ directory. Any process with filesystem access to that directory could trigger it — this is by design (remote SSH trigger per D-03). Authentication is the OS/user boundary, not the EA's responsibility. |
| T-01-06 | Spoofing | IPCReader.mqh | mitigate | Staleness check (timestamp comparison per D-06) prevents EA from using stale params. If params file timestamp > InpParamsStalenessSeconds old, EA falls back to safe defaults. |
</threat_model>

<verification>
After all tasks complete:
1. Verify all Python tests pass: `python -m pytest tests/ea/ -v`
2. Verify EA compiles: copy `ea/` directory to `{MT5_DATA}/MQL5/Experts/Futra/`, compile in MetaEditor (or verify syntax via MT5 compiler CLI if available)
3. Verify kill switch contract: write kill_switch.json via test_helpers → validate JSON structure matches contract
4. Verify params contract: write {SYMBOL}_params.json via test_helpers → validate JSON structure matches contract
5. Verify trade log contract: validate JSONL format matches TradeResult struct
</verification>

<success_criteria>
- [ ] EA skeleton compiles without errors in MT5 MetaEditor
- [ ] Kill switch reads file-based signal and halts trading (contract verified via Python test)
- [ ] Market order execution creates buy/sell orders with SL/TP (contract verified via test)
- [ ] Per-symbol params files read with staleness check (contract verified via test)
- [ ] EA falls back to hardcoded safe defaults when params missing/stale (contract verified via test)
- [ ] Every trade result logged to JSONL file with all required fields (contract verified via test)
- [ ] All `python -m pytest tests/ea/ -v` tests pass (no MT5 connection needed)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-safety/01-01-SUMMARY.md`
</output>
